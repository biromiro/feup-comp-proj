import ioPlus;

class HelloWorld {
    public static void main(String[] args) {
        int x;
        int i;
        x = 1;
        i = 0;
        while(i < 6) {
            if (1 < i && i < 4) {
                x = 100 + i;
            } else {}

            ioPlus.printResult(x);
            i = i + 1;
        }
    }
}