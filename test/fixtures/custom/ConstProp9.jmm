import ioPlus;
class ConstProp {

    public int fun(int i) {
        int x;
        x = 50;

        if (i < 10) {
            ioPlus.printResult(x);
        } else {
            ioPlus.printResult(x);
        }

        return 0;
    }

	public static void main(String[] args) {
	    new ConstProp().fun(1);
	}
}
