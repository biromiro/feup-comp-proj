import ioPlus;
class HelloWorld {
    int a;

    public int calcaux(int x) { return x+1; }
        public int calc(int[] abc, boolean calc) {
        int[] d;
        HelloWorld hw;
        int a;
        boolean what;
        d = new int[abc.length + 2];
        abc = new int[3];
        hw = new HelloWorld();
        abc = hw.calcaux(abc[5 + 3]);
        abc[0] = 2;
        a = abc[1];
        what = !calc && false;
        calc = a < abc[0];
        return d.length;
    }

    public static void main(String[] args) {
        int a;
        int b;
        int c;
        int[] julia;
        HelloWorld hello;
        hello =  new HelloWorld();
        a = 10*3-4+1;
        b = c + 3 * 2;
        c = a + c * 2 + 1;
        c = c / 2 + 3 * ( 2 + 1 * 3);
        hello.calc(julia, true);
        this.calc(julia, false);
        ioPlus.printHelloWorld();
    }
}