import ioPlus;
class HelloWorld {

    public boolean val(int a, boolean calc) {
        if (calc && a < 2 && false) {
            a = 20;
            calc = a < 3;
        } else {
            a = 3;
        }
        calc = a < 1;
        return calc;
    }

    public static void main(String[] args) {
        int a;
        a = 2;
        this.val(a, true);
    }
}