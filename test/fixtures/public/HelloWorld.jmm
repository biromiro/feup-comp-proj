import io;
class HelloWorld {

    public int func() {
        int a;
        int b;
        int c;
        a = 30;
        b = 9 - a / 5;
        c = b * 4;
        return c;
    }

    /* eliminates only a few lines of code */
    public int func(int d) {
        int a;
        int b;
        int c;
        a = 30;
        b = 9 - a / 5;
        c = b * 4;
        if (10 < d) {
            c = c - 10;
        } else {
            c = 15;
        }
        while (10 < c) {
            c = c + 1;
        }
        return c * (60 / a);
    }

    public static void main(String[] args) {
        HelloWorld hw;
        hw = new HelloWorld();
        hw.func();
      }
}